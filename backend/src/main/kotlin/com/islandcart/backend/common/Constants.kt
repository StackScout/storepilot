package com.islandcart.backend.common

import java.math.BigDecimal

/** Mirrors app/src/lib/constants.ts — keep these two values in sync by hand. */
val PLATFORM_FEE_PERCENT: BigDecimal = BigDecimal("3.5")
const val FLAT_SHIPPING_FEE_LKR: Int = 350
